# ------------------------------------------------------------------------------
# Archivo de ejemplo de configuración de entorno (.env.example)
# 
# Este archivo contiene las variables de entorno necesarias para la configuración
# y funcionamiento del sistema gestor de indicadores académicos.
# Organizado por secciones para facilitar su comprensión y personalización.
# ------------------------------------------------------------------------------

# ------------------------------------------------------------------------------
# Configuración de la Aplicación
# ------------------------------------------------------------------------------
APP_NAME="Sistema gestor indicadores académicos" # Nombre de la aplicación.
APP_ENV=local # Entorno de ejecución (local, production, testing, etc).
APP_KEY=base64:kJSaTekemwo4GhRJmCuBsGrn3UaI2S1EZbTVQc4KWXU= # Clave de cifrado base64 para la aplicación.
APP_DEBUG=true # Habilita/deshabilita el modo debug (true/false).
APP_URL=http://localhost # URL base de la aplicación.

# ------------------------------------------------------------------------------
# Configuración de Localización
# ------------------------------------------------------------------------------
APP_LOCALE=es  # Idioma principal de la aplicación (ej: es, en).
APP_FALLBACK_LOCALE=es # Idioma de respaldo cuando falta traducción.
APP_FAKER_LOCALE=es_ES # Idioma para generación de datos ficticios.
APP_TIMEZONE=America/Mexico_City # Zona horaria de la aplicación.

# ------------------------------------------------------------------------------
# Mantenimiento
# ------------------------------------------------------------------------------
APP_MAINTENANCE_DRIVER=file # Driver para modo mantenimiento (file/database).
# APP_MAINTENANCE_STORE=database # Almacenamiento alternativo para mantenimiento (opcional).

# ------------------------------------------------------------------------------
# Configuración de PHP
# ------------------------------------------------------------------------------
PHP_CLI_SERVER_WORKERS=4 # Número de workers para el servidor CLI de PHP.

# ------------------------------------------------------------------------------
# Seguridad y Logs
# ------------------------------------------------------------------------------
BCRYPT_ROUNDS=12 # Número de rondas para el hash bcrypt (costo computacional).
LOG_CHANNEL=stack # Canal principal de logs (stack, single, daily, stderr, etc).
LOG_STACK=single # Configuración de stack para logs.
LOG_DEPRECATIONS_CHANNEL=null # Canal para logs de deprecaciones (null para desactivar).
LOG_LEVEL=debug # Nivel de detalle de logs (debug, info, notice, warning, error, critical, alert, emergency).

# ------------------------------------------------------------------------------
# Configuración de Base de Datos (MongoDB)
# ------------------------------------------------------------------------------
DB_CONNECTION=mongodb # Tipo de conexión (mongodb, mysql, pgsql, sqlsrv, etc).
DB_HOST=127.0.0.1 # Host/IP del servidor de base de datos.
DB_PORT=27017 # Puerto de conexión a la base de datos.
DB_DATABASE=indicadores-academicos # Nombre de la base de datos.
DB_USERNAME=root # Usuario para autenticación en la base de datos.
DB_PASSWORD= # Contraseña del usuario (dejar vacío si no aplica).

# ------------------------------------------------------------------------------
# Sesiones, Colas y Almacenamiento
# ------------------------------------------------------------------------------
SESSION_DRIVER=array # Driver para manejo de sesiones (file, cookie, database, array, etc).
BROADCAST_CONNECTION=log # Driver para broadcasting (pusher, redis, log, null).
FILESYSTEM_DISK=local # Disco principal para almacenamiento de archivos (local, s3, etc).
QUEUE_CONNECTION=database # Driver para colas de trabajo (sync, database, redis, beanstalkd, sqs).

# ------------------------------------------------------------------------------
# Cache
# ------------------------------------------------------------------------------
CACHE_STORE=database # Driver para cache (file, array, database, redis, memcached).
# CACHE_PREFIX= # Prefijo opcional para claves de cache (evita colisiones).

# ------------------------------------------------------------------------------
# Memcached
# ------------------------------------------------------------------------------
MEMCACHED_HOST=127.0.0.1 # Host/IP del servidor Memcached.

# ------------------------------------------------------------------------------
# Redis
# ------------------------------------------------------------------------------
REDIS_CLIENT=phpredis # Cliente de Redis (phpredis o predis).
REDIS_HOST=127.0.0.1 # Host/IP del servidor Redis.
REDIS_PASSWORD=null # Contraseña de Redis (null si no requiere autenticación).
REDIS_PORT=6379 # Puerto de conexión a Redis.

# ------------------------------------------------------------------------------
# Configuración de Correo
# ------------------------------------------------------------------------------
MAIL_MAILER=log # Driver para envío de correos (smtp, sendmail, mailgun, log, etc).
MAIL_SCHEME=null # Esquema de conexión (tls, ssl, null).
MAIL_HOST=127.0.0.1 # Host del servidor SMTP.
MAIL_PORT=2525 # Puerto del servidor SMTP.
MAIL_USERNAME=null # Usuario para autenticación SMTP.
MAIL_PASSWORD=null # Contraseña para autenticación SMTP.
MAIL_FROM_ADDRESS="hello@example.com" # Dirección de correo remitente por defecto.
MAIL_FROM_NAME="${APP_NAME}" # Nombre del remitente por defecto.

# ------------------------------------------------------------------------------
# AWS S3
# ------------------------------------------------------------------------------
AWS_ACCESS_KEY_ID= # Clave de acceso AWS (ID de clave de acceso).
AWS_SECRET_ACCESS_KEY= # Clave secreta AWS (secreto de la clave).
AWS_DEFAULT_REGION=us-east-1 # Región por defecto de AWS.
AWS_BUCKET= # Nombre del bucket S3.
AWS_USE_PATH_STYLE_ENDPOINT=false # Usar endpoint de estilo path (true/false).

# ------------------------------------------------------------------------------
# Vite (Bundler para frontend)
# ------------------------------------------------------------------------------
VITE_APP_NAME="${APP_NAME}" # Nombre de la aplicación para Vite.

# ------------------------------------------------------------------------------
# Configuración de Autenticación (Sanctum)
# ------------------------------------------------------------------------------
AUTH_GUARD="api" # Guard de autenticación a utilizar (web, api, etc).